import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    borderWidth: 1,
    borderColor: 'gray',
    paddingVertical: 10,
    alignItems: 'center',
    width: '100%',
    marginBottom: 45
  },
  signupText: {
    color: 'hotpink',
  },
});

export default styles;
